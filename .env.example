# Local dev defaults (used by docker-compose)
POSTGRES_USER=ecom
POSTGRES_PASSWORD=ecom_pw

# Required for scripts/generate-images.py
OPENAI_API_KEY=sk-...

# GCP deploy targets (used by build-and-push.sh / deploy.sh)
GCP_PROJECT_ID=your-gcp-project
GCP_REGION=us-central1
ARTIFACT_REPO=ecom-microservices
