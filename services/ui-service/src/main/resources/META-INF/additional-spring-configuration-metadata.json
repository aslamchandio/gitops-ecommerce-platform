{
  "properties": [
    {
      "name": "app.version",
      "type": "java.lang.String",
      "description": "Build version (typically the short git SHA, injected via APP_VERSION env). Appended as ?v=<value> to static asset links so a new release invalidates browser caches automatically.",
      "defaultValue": "dev"
    }
  ]
}
